# AI CEO Circle

Landing page built with Next.js, React, and Tailwind CSS.

## Development

Install dependencies:

```powershell
pnpm install
```

Run the local development server:

```powershell
pnpm dev
```

Build for production:

```powershell
pnpm build
```
